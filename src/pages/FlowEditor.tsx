import React from 'react';

const FlowEditor: React.FC = () => {
    return (
        <div>
            <h1>Flow Editor</h1>
            {/* Adicione seu código do Flow Editor aqui */}
        </div>
    );
};

export default FlowEditor;